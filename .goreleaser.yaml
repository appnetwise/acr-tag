project_name: acr-tag
before:
  hooks:
    - go mod download
builds:
  - main: .
    binary: acr-tag
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
    goarch:
      - amd64
    ldflags:
      - -s -w -X "tag.GitRelease={{.Version}}"
archives:
  - format_overrides:
    name_template: "{{ .ProjectName }}-{{ .Os }}-{{ .Arch }}"
checksum:
  name_template: "checksums.txt"
changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
